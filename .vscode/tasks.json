{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Prisma generate and migrate",
			"type": "shell",
			"command": "pwsh",
			"args": [
				"-NoProfile",
				"-Command",
				"$ErrorActionPreference = 'Stop'; if (-Not (Test-Path .env)) { Copy-Item .env.example .env }; pnpm approve-builds --all; npx prisma generate; npx prisma migrate dev --name init_auth --create-only"
			]
		}
	]
}